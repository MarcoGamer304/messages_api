export type TConfig = {
  DATABASE: string;
  USER: string;
  PASSWORD: string;
  HOST: string;
  JWT_SECRET: string;
  JWT_EXPIRATION: string;
  NODE_ENV: string;
  CORS_ORIGIN: string;
  PORT: string;
  URL: string;
};
